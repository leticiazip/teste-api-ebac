{
  "name": "teste-api-ebac",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start-server": "npx serverest --porta 3500",
    "cy:run": "npx cypress run",
    "ci": "npx start-server-and-test start-server http://localhost:3500 cy:run"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress-plugin-api": "^2.11.1",
    "joi": "^17.12.0"
  },
  "devDependencies": {
    "cypress": "^13.12.0",
    "start-server-and-test": "^2.0.4"
  }
}